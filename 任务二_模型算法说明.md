# 任务二：三目标精确 Pareto 前沿

目标固定为 `c1=distance`、`c2=travel_time`、`c3=elevation`。对每个 `(s,t)`，输出所有不同且不被其他向量支配的可行目标向量。

## 精确算法

`task2_solver.py` 对每个城市只读取一次统一边文件。每个节点维护非支配 label 列表和向量集合；label 为从源点到该节点的三维累计成本。优先队列弹出 label 后沿邻接表扩展。新 label 若被已有 label 支配或重复则立即丢弃；否则删除它严格支配的旧 label 并入队。算法结束时终点 label 集即为完整 Pareto 前沿。

代码还从终点对反图分别运行三次单目标 Dijkstra，得到三个安全下界 `LB_j(v)`。若已有终点向量逐分量支配 `label+LB(v)`，该 label 不可能产生新的终点 Pareto 解，可以安全剪枝；这不会删除任何精确前沿。优先级使用三项和仅影响处理顺序，不改变结果。

## 正确性思路

归纳地，所有入队 label 都对应一条合法源到当前节点的 walk，扩展保持合法性。被当前节点 label 支配的 label 加上任意共同后缀后仍被支配，因此可删除；被终点已知向量支配其安全下界完成向量的 label 也不可能成为终点非支配解。反之，任一终点 Pareto 路径的每个前缀若被删除，都存在不劣前缀；替换该前缀并接上原后缀不会变差，所以最终仍有不劣终点向量。最终队列耗尽后，终点无重复向量且互不支配，故得到精确完整前沿。

## 复杂度、瓶颈和优化

设每节点最多保留 K 个非支配 label。朴素 dominance 检测每次为 `O(K)`，总体约为 `O(L·(K+d))`，其中 L 是生成 label 数、d 是平均出度；三次下界 Dijkstra 为 `O(3(m+n)log n)`。内存为 `O(m+n+L)`。真正最可能爆炸的位置是相关性弱、道路绕行多时的 label 数 L，而不是堆本身。

工程上使用紧凑整数节点 ID、邻接数组、每节点 list+set 去重、终点早期下界剪枝、反图一次构建和按城市复用图。进一步可按第一坐标排序维护二维前沿、使用 Fenwick/分块结构做二维支配查询，或缓存相同终点的下界；这些只是加速 dominance，不改变剪枝规则。不能用任意 beam width、label 截断或加权和代替，因为会破坏完整性。

## 验证

结果 CSV 按 `(dataset,query_id)` 检查：`solution_id` 必须从 1 连续；三维向量不能重复；任意两向量不能互相支配。小图上可枚举所有简单路径，将其三维向量去重并删除被支配向量，与算法结果集合逐项比较，这同时验证“无漏解”。大图可额外检查每条输出向量由合法路径生成（若保留 predecessor），并记录每个节点的 label 数峰值、终点前沿大小和剪枝数。

运行：

```bash
python task2_solver.py --researcher 123
```
