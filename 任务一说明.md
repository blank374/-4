# 任务一说明

对每个城市建立有向图 G=(V,E)。边 e 的非负向量为 w(e)=(distance, travel_time, elevation, avg_degree, hop_count)。对每个查询 (s,t) 和每个目标 j，求合法路径 P，使 `min Σ wj(e)`；五个目标分别独立优化，不使用加权和近似。

程序把统一五权重边文本读取一次为邻接表 `adj[u]`，每个 `(query, objective)` 使用 `heapq` 实现的 Dijkstra。父节点表支持完整回溯；回溯后验证无重复节点、每一对相邻节点存在有向边，并重新从边记录累计 c1-c5，因此输出不依赖内部距离值。等价最优路径任取一条。

设 n=|V|、m=|E|，一次查询一次目标的时间复杂度为 O((n+m)log n)，空间复杂度 O(n+m)；100 组查询和 5 个目标最多 500 次搜索，图只加载一次。

三城市冲突分析：同一 query_id 下，将五种 objective 的结果两两比较。对目标路径 Pi 在指标 k 上定义绝对差 `Δ(i,j,k)=c_k(Pi)-c_k(Pj)`、相对差 `R=Δ/(c_k(Pj)+ε)`；报告每个城市的平均值、中位数、P90、R>0 的冲突率和 Pareto 支配率。另报告各目标路径长度、不可达数、运行时间，并以五条候选路径在指标 k 上的最小值计算 regret：`(c_k(Pi)-min_q c_k(Pq))/max(min_q c_k(Pq),ε)`。比较 NY、BAY、COL 的均值和分位数即可说明网络规模与道路属性之间的冲突程度。

运行：

```bash
python task1_solver.py --data-dir . --output-dir results
```

本项目默认读取 `data/edges/edges_{NY,BAY,COL}_5obj.txt` 和 `data/dimacs5_{ny,bay,col}/queries_problem1.csv`，并生成一个 `result1_研XXX.csv`；`XXX` 可通过 `--researcher` 指定。
