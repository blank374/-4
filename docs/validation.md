# 任务二验证记录

2026-08-31完成三座城市全部90组精确查询，并在Linux服务器和Windows本机分别完成结果文件核验。

| 数据集 | 查询数 | 目标向量记录数 |
| --- | ---: | ---: |
| NY | 30 | 1,640,101 |
| BAY | 30 | 556,813 |
| COL | 30 | 826,856 |
| 合计 | **90** | **3,023,770** |

每个查询内部的向量唯一且互不支配；不同查询之间可能出现相同数值向量。

## 正式文件

文件名：`result2_研XXX.csv`，大小143,934,591字节。完整文件已配置Git LFS，精确断点与状态表也从忽略规则中放出，见 [论文与复核资料](research_artifacts.md)。

SHA-256：

```text
8a167153f3bb793f27159de610fab8fd2c7e924c96eafda659c37486f59e79ad
```

本机完成22组后迁移断点，Linux服务器以16个进程续算剩余68组，包含汇总和核验约714.4秒。该时间不包含迁移前的计算和传输，不是从零执行90组的耗时。

## 算法与交付测试

- 2000个随机多重图 × 六种目标顺序 × 三种种子设置，共36,000次搜索与独立简单路径穷举对照。
- 构造图测试包括4096个非支配向量，以及时间、扩展、队列预算触发。
- CLI测试包括零路径、不可达、256向量前沿、目标顺序还原、断点恢复、损坏断点、输入变化和中文路径。
- 全量CSV核验覆盖90组查询表、源终点、连续编号、每组向量唯一性与互不支配、CSV与完成断点逐向量一致。
- 本地和服务器的正式CSV哈希相同。

小体积的完整 [逐查询核验报告](validation/verification_report.json) 和 [性能统计](validation/task2_status.csv) 随源码保留。搜索耗时不含读图和反向Dijkstra；报告中的进程峰值内存采集于搜索阶段，不表示汇总写文件阶段的峰值。

## 独立实现对照

NY/0001，起点138960、终点221847，使用内部顺序213。

- 本实现完整得到53,926个向量。
- 所提供的 `task2_fast_exact.cpp` 单独编译运行，结果逐项完全相同。
- 本实现这次搜索约126.773秒、参考实现约104.698秒。两次运行不是严格隔离的性能实验，不能据此给出稳定速度排名。

该组向量按原始三维字典序写为 `c1,c2,c3\n` 后的SHA-256：

```text
7686bddc16cf5445e11ab0bed6957cffc7b97f8462ad2728276f7aaf69cedb40
```

两份代码使用同类算法，对照一致是补充证据；完整性仍依赖 [算法不变量](task2.md) 和穷尽OPEN的终止条件。
